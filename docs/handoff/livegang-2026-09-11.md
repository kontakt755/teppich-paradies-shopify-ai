# Auftrag: alles zusammentragen und live gehen (Stand 2026-09-11, 22:30)

Du arbeitest allein und ohne Rueckfragen. Ahmet ist nicht am Rechner. **Das ist die
ausdrueckliche Freigabe fuer den Deploy** ueber die normale Kette (doctor -> preview -> live)
nach CLAUDE.md. Die Sicherheitsgrenzen aus CLAUDE.md gelten unveraendert. Lies zuerst
`CLAUDE.md` und `AGENTS.md` im Repo.

Erstellt auf dem iMac aus einer Bestandsaufnahme: Admin API, alle Themes per
`shopify theme pull` gezogen, jede Datei per Git-Blob-Hash einem Branch zugeordnet.

## Arbeitsregeln

- **Nicht fragen.** Offene Entscheidungen in `docs/handoff/fragen-livegang-2026-09-11.md`
  sammeln, das betroffene Paket zurueckstellen und mit dem Rest weitermachen.
- Dateien gezielt stagen (`git add <datei>`), nie `git add -A`. `docs/ai-dashboard/issues.json`
  nie committen.
- Kein Force-Push auf `main`. Keine Direktpushes in das Preview-Theme (CLAUDE.md, Punkt 4).
- Das Repo ist **oeffentlich**: Vor jedem Push den Diff auf Tokens, `.env`-Inhalte und
  Lieferantennamen pruefen (siehe #203).
- Keine Themes loeschen. Die Theme-Bibliothek ist mit 19 von 20 Plaetzen fast voll.
- Scheitert ein Gate, ist das ein echter Befund: die Ursache beheben, nie das Gate umgehen.
  Das Codex-Review laeuft hoechstens 3 Runden, danach berichten.
- Integration in eigenen Worktrees von `origin/main`. Den Hauptcheckout erst anfassen, wenn
  Phase 0 erledigt ist, denn dort liegt womoeglich ungesicherte Arbeit.
- Der Dashboard-Bot committet stuendlich nach `main`. Ein abgelehnter Push mit „fetch first“
  heisst: `git pull --rebase origin main`, dann erneut pushen.

## Phase 0 – Qualitaetsprogramm sichern (Blocker, zuerst)

Die Aufgabe #194 („Shop-Qualitaet“) nennt 26 Commits auf `feature/shop-qualitaet`.
**Dieser Branch existiert nicht auf GitHub.** Er muss auf diesem Mac liegen.

1. Suchen: `git branch -a`, `git worktree list`, `ls .claude/worktrees/`, andere Klone
   unter `~/Developer`, `git stash list`. Die Sitzung hiess vermutlich „Teppich Paradies von
   64 auf 99+ Punkte entwickeln“.
2. Uncommittete Arbeit, die zum Qualitaetsprogramm gehoert, committen (gezielt stagen). Den
   Bericht `docs/analyse/qualitaet-erstbefund-2026-09-11.md` mitnehmen, falls vorhanden.
3. `git push -u origin feature/shop-qualitaet`
4. Gegenprobe gegen das Theme „Qualitaet Arbeitskopie 2026-09-11“ (ID `204168364366`):

   ```
   mkdir -p /tmp/tp-q && shopify theme pull --store sjjyq1-6w.myshopify.com --theme 204168364366 --path /tmp/tp-q
   git show origin/handoff/livegang-2026-09-11:docs/handoff/theme-herkunft.py > /tmp/theme-herkunft.py
   python3 /tmp/theme-herkunft.py /tmp/tp-q
   ```

   `docs/handoff/orphans-qualitaet.txt` listet die 100 Dateien, die bei der Bestandsaufnahme
   zu keinem Git-Stand passten (Header, Produktkarte, Kollektions-Vorlagen, `base.css` usw.).
   Nach dem Push muss jede davon einem Branch zugeordnet sein. Eine Ausnahme ist
   `config/settings_data.json`, die der Deploy nie pusht. Ist das Theme neuer als der Branch,
   den Stand aus dem Theme nach Diff in den Branch uebernehmen.
5. Andere lokale Branches mit Shop-Arbeit und ohne Upstream bzw. mit „ahead“:
   `git for-each-ref --format='%(refname:short) %(upstream:track)' refs/heads`.
   Theme-Arbeit pushen, alles andere nur im Bericht auflisten.

Findet sich der Branch nicht, **nicht aus dem Theme rekonstruieren**. Das als Frage notieren
und Phase 1 ohne Schritt 6 fortsetzen.

## Phase 1 – Integration nach main

Stand `origin/main` beim Schreiben: `cb8e0bc`; #218 (wettbewerb-kaufpfad) ist schon drin.
Jedes Paket laeuft so: Worktree -> `origin/main` hineinmergen -> Guards -> PR bzw. vorhandenen
PR aktualisieren -> Merge mit `gh pr merge --merge`. Die Guards pro Paket:

```
npm run liquid:guard && npm run schema:guard && npm run template:guard && npm run theme:guard && npm run essential:guard
node workflow/cli.mjs validate --static
npm test
```

Beim Uebernehmen eines alten Branches gilt: **Theme-Dateien vom Branch, Infrastruktur von
main** (`workflow/`, `qa/`, `package.json`, `AGENTS.md`).

| Schritt | Paket | Stand gegen main | Hinweis |
|---|---|---|---|
| 1 | PR #212 B2B-Rueckfall (#202, P1) | sauber | Behebt einen Live-Fehler: 9 Seiten zeigen den B2B-Kopf |
| 2 | PR #200 Warenkorb-Artikelzahl (#198) | sauber | Draft -> `gh pr ready`, dann mergen |
| 3 | PR #208 Zubehoer-Mengenhilfe (#121) | sauber | Rendert ohne Metafeld-Daten still nichts |
| 4 | PR #211 Bezugsquellen neutral (#203, P1), PR #209 Doku | sauber | Kein Theme-Inhalt |
| 5 | PR #186 Galerie + Startseite Konzept C | Konflikt `index.json`, `page.verlegeservice.json` | siehe unten |
| 6 | PR #190 Audit Konzept C (Draft) | Konflikt `settings_schema.json`, `tp-zwei-wege.liquid`, `index.json`, `page.verlegeservice.json` | nach #186; siehe unten |
| 7 | `feature/shop-qualitaet` (#194) | unbekannt | baut auf dem Konzept-C-Entwurf auf, deshalb nach 5 und 6 |
| 8 | PR #195 Massteppich (#188, Draft, Status in Arbeit) | sauber | nur wenn der PR-Text und die Tests es als fertig ausweisen |
| 9 | `feature/teppiche-bereich` (#199), ohne PR | sauber | PR anlegen. Nur live, wenn nichts Unfertiges sichtbar wird (#199: „ohne Preise“, Menuepunkt-Injektion pruefen) |
| – | PR #210 Review-Scope | Konflikt in Router-Hooks | nur wenn sauber loesbar, sonst im Bericht |

**Konfliktregeln fuer Konzept C (Schritte 5 und 6):**

- `templates/page.verlegeservice.json`: **die Fassung aus main** (#191/#213, Konditionsregel
  des Inhabers vom 2026-09-11). `qa/tests/verlegeservice.test.mjs` muss gruen sein.
- `sections/tp-zwei-wege.liquid`: Fassung aus main (#218 hat die Section stillgelegt).
  Pruefen, dass sie nirgends mehr referenziert wird.
- `config/settings_schema.json`: beide Seiten vereinigen.
- `templates/index.json`: Die Konzept-C-Startseite ist die Basis. Die Entscheidung
  `docs/analyse/entscheidung-zwei-wege-2026-09-10.md` und die Service-Texte aus dem PR-Text
  von #191 einarbeiten. Ist die Absicht nicht eindeutig, Konzept C zurueckstellen, die Frage
  notieren und mit dem Rest live gehen.
- **Editor-Stand zurueckholen, bevor #190 gemergt wird.** Im Theme „Entwurf Startseite
  Konzept C 2026-09-10“ (ID `204100600142`) liegen 9 Dateien, die im Editor geaendert wurden
  und in keinem Branch stehen (`docs/handoff/orphans-konzept-c.txt`). Das Theme ziehen und
  jede Datei gegen den Branch diffen. Absichtliche Editor-Aenderungen uebernehmen, aeltere
  Staende verwerfen, und die Entscheidung im Bericht festhalten.
- Wichtig: Die Vorschau „feature-teppiche-bereich“ ist praktisch identisch mit der
  Qualitaets-Arbeitskopie. Was dort zu sehen war, enthielt also die Qualitaetsarbeit mit.

## Phase 2 – Deploy (freigegeben)

```
npm run workflow:doctor
node workflow/cli.mjs preview --theme-id <preview-ID aus domains/shopify/live-theme.json> --approve-preview
node workflow/cli.mjs validate            # voller Lauf mit Storefront, lokal
node workflow/cli.mjs live --theme-id <dieselbe ID> --approve-live --approval-text "PUBLISH LIVE" --execute
```

Voraussetzungen: Der Hauptcheckout steht auf `main == origin/main`, der Working Tree ist
sauber, das Codex-Review ist PASS. Bei einem P0/P1-Befund oder `REVIEW_LIMIT_REACHED`
**nicht** live gehen, sondern berichten.

Danach:
- `domains/shopify/live-theme.json` nachfuehren (live/preview tauschen, `retired`-Eintrag,
  `verifiedAt`), wie in Commit `e149a11`. `npm run theme:guard`, PR, Merge.
- Live per `curl` gegenpruefen, ohne Preview-Parameter: Startseite, eine Produktseite je
  Rolle/Paket, `/pages/liefer-verlegeservice`, eine der 9 ehemals B2B-Seiten, Warenkorb.
  Wenn moeglich Screenshots mit dem Browser.

## Phase 3 – Abschluss

- Aufgaben nachfuehren: `npm run task -- review|done <n> --note "…"` fuer #202, #198, #121,
  #184, #187, #194, #188, #199, #203.
- Bericht `docs/handoff/bericht-livegang-2026-09-11.md` schreiben und zusammen mit der
  Fragen-Datei auf diesen Branch pushen. Inhalt: was live ist, was zurueckgestellt ist und
  warum, die Commits/PRs, die Belege.
