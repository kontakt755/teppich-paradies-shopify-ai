# SHP-008 – Notification Policy

Status: **PASS**

- Providerneutraler Adapter unter `automation/core/**` implementiert.
- Nur `RUN_COMPLETE`, `HARD_STOP`, `ALLOWLIST_VIOLATION`, `PROVIDER_SWITCH`, `NEEDS_AHMET` und `CRITICAL_BLOCKER` lösen Benachrichtigung/Sound aus.
- Normale erfolgreiche Tasks bleiben still.
- Dry Run, Eventtyp, Task-ID, kurze redigierte Nachricht und Längenlimit sind umgesetzt.
- Servicefehler werden ohne Loginhalt protokolliert und als `FAILED` zurückgegeben; State und Working Tree werden nicht berührt.
- Tests verwenden ausschließlich Event-Fixtures und Dependency-Injection. Kein echter ntfy-Aufruf in Tests.
- `C:\Users\Administrator\.codex\ntfy-notify.cjs` wurde nicht verändert.

Live-/Fallback-/Dev-Theme und Shopify-Daten wurden nicht verändert.
