# SHP-006 – Diff-Budget Guard

Status: **PASS**

- Deterministische Prüfung von `MAX_FILES`, `MAX_CHANGED_LINES` und `ALLOWED_FILES` implementiert.
- Budgetüberschreitung ergibt `REVIEW_REQUIRED`; Allowlist-, unbekannte Datei- und höhere Risikoklasse ergeben `HARD STOP`.
- Git-Numstat wird ohne LLM ausgewertet; Binärdateien zählen als Datei, aber nicht als Textzeilen.
- `automation/reports/**` und `automation/state/**` werden auf Allowlist/Risiko geprüft, jedoch nicht dem Implementierungsbudget zugerechnet.
- Fixtures und Unit-Tests decken PASS, beide Budgetgrenzen und alle Hard-Stop-Klassen ab.

Live-/Fallback-/Dev-Theme und Shopify-Daten wurden nicht verändert.
